{
  "hash": "59b736be0f09651978d85f029146cfd5",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Hands-on Exercise 9c\"\nauthor: \"Serene Cheng\"\ndate: \"June 19, 2026\"\ndate-modified: \"last-modified\"\nexecute:\n  eval: true\n  echo: true\n  warning: false\n  freeze: true\nformat:\n  html:\n    code-fold: true\n    code-summary: \"Show the code\"\n---\n\n# Reflections\n\nI learned that mapping counts directly can be misleading because the underlying phenomenon, in this case water points, is not evenly distributed in space; mapping rates (e.g. percentage functional) instead of raw counts gives a fairer picture. I also learned how extreme value maps such as percentile maps and box maps are designed to highlight outliers at the low and high ends of a distribution, which is a useful complement to standard choropleth classification methods like quantile or equal. Writing small reusable R functions such as `get.var()`, `percentmap()`, `boxbreaks()` and `boxmap()` also showed me how functions reduce repetition and the risk of copy-paste mistakes when the same logic needs to be applied to multiple variables.\n\n# Introduction\n\nIn this exercise, we gain hands-on experience using appropriate R methods to plot analytical maps. By the end of this exercise, we will be able to use functions of **tmap** and **tidyverse** to:\n\n-   Import geospatial data in rds format into the R environment.\n\n-   Create cartographic quality choropleth maps using appropriate **tmap** functions.\n\n-   Create a rate map.\n\n-   Create a percentile map.\n\n-   Create a box map.\n\n# Getting Started\n\n## Installing and loading packages\n\n\n::: {.cell}\n\n```{.r .cell-code}\npacman::p_load(tmap, tidyverse, sf)\n```\n:::\n\n\n## Importing data\n\nFor this exercise, a prepared data set called `NGA_wp.rds` will be used. The data set is a polygon feature data frame providing information on water points of Nigeria at the LGA level.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nNGA_wp <- read_rds(\"data/NGA_wp.rds\")\n```\n:::\n\n\n# Basic Choropleth Mapping\n\n## Visualising distribution of non-functional water point\n\nThe code chunk below plots a choropleth map showing the distribution of functional and total water points by LGA, side by side.\n\n\n::: {.cell}\n\n```{.r .cell-code}\np1 <- tm_shape(NGA_wp) +\n  tm_polygons(fill = \"wp_functional\",\n              fill.scale = tm_scale_intervals(\n                style = \"equal\",\n                n = 10,\n                values = \"brewer.blues\"),\n              fill.legend = tm_legend(\n                position = c(\"right\", \"bottom\"))) +\n  tm_borders(lwd = 0.1,\n             fill_alpha = 1) +\n  tm_title(\"Distribution of functional water point by LGAs\")\n\np2 <- tm_shape(NGA_wp) +\n  tm_polygons(fill = \"total_wp\",\n              fill.scale = tm_scale_intervals(\n                style = \"equal\",\n                n = 10,\n                values = \"brewer.blues\"),\n              fill.legend = tm_legend(\n                position = c(\"right\", \"bottom\"))) +\n  tm_borders(lwd = 0.1,\n             fill_alpha = 1) +\n  tm_title(\"Distribution of total water point by LGAs\")\n\ntmap_arrange(p2, p1, nrow = 1)\n```\n\n::: {.cell-output-display}\n![](Hands-on_Ex09c_files/figure-html/unnamed-chunk-3-1.png){width=672}\n:::\n:::\n\n\n# Choropleth Map for Rates\n\nIt is important to map rates rather than counts, because water points are not equally distributed in space. If we do not account for how many water points are present in an area, we end up mapping the size of the water point network rather than our actual topic of interest.\n\n## Deriving Proportion of Functional Water Points and Non-Functional Water Points\n\nThe code chunk below uses `mutate()` of **dplyr** package to derive two new fields, *pct_functional* and *pct_nonfunctional*.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nNGA_wp <- NGA_wp %>%\n  mutate(pct_functional = wp_functional / total_wp) %>%\n  mutate(pct_nonfunctional = wp_nonfunctional / total_wp)\n```\n:::\n\n\n## Plotting map of rate\n\nThe code chunk below plots a choropleth map showing the distribution of the percentage of functional water points by LGA.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntm_shape(NGA_wp) +\n  tm_polygons(\"pct_functional\",\n              fill.scale = tm_scale_intervals(\n                style = \"equal\",\n                n = 10,\n                values = \"brewer.blues\"),\n              fill.legend = tm_legend(\n                position = c(\"right\", \"bottom\"))) +\n  tm_borders(lwd = 0.1,\n             fill_alpha = 1) +\n  tm_title(\"Rate map of functional water point by LGAs\")\n```\n\n::: {.cell-output-display}\n![](Hands-on_Ex09c_files/figure-html/unnamed-chunk-5-1.png){width=672}\n:::\n:::\n\n\n# Extreme Value Maps\n\nExtreme value maps are variations of common choropleth maps where the classification is designed to highlight extreme values at the lower and upper end of the scale, with the goal of identifying outliers. These maps were developed in the spirit of spatialising exploratory data analysis (EDA) - that is, adding spatial features to commonly used approaches in non-spatial EDA.\n\n## Percentile Map\n\nThe percentile map is a special type of quantile map with six specific categories: 0-1%, 1-10%, 10-50%, 50-90%, 90-99%, and 99-100%. The corresponding breakpoints can be derived using the base R `quantile()` function, passing an explicit vector of cumulative probabilities as `c(0, .01, .1, .5, .9, .99, 1)`. Note that the begin and end points need to be included.\n\n### Data Preparation\n\nStep 1: Exclude records with NA.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nNGA_wp <- NGA_wp %>%\n  drop_na()\n```\n:::\n\n\nStep 2: Create a customised classification and extract the values.\n\n\n::: {.cell}\n\n```{.r .cell-code}\npercent <- c(0, .01, .1, .5, .9, .99, 1)\nvar <- NGA_wp[\"pct_functional\"] %>%\n  st_set_geometry(NULL)\nquantile(var[, 1], percent)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n       0%        1%       10%       50%       90%       99%      100% \n0.0000000 0.0000000 0.2169811 0.4791667 0.8611111 1.0000000 1.0000000 \n```\n\n\n:::\n:::\n\n\n**Note:** When variables are extracted from an `sf` data frame, the geometry is extracted as well. For mapping and spatial manipulation this is expected behaviour, but many base R functions cannot deal with the geometry - specifically, `quantile()` will return an error. As a result, `st_set_geometry(NULL)` is used to drop the geometry field.\n\n### Why writing functions?\n\nWriting a function has three big advantages over copy-and-paste:\n\n-   It can be given an evocative name that makes the code easier to understand.\n\n-   As requirements change, code only needs to be updated in one place instead of many.\n\n-   It eliminates the chance of making incidental mistakes when copying and pasting (e.g. updating a variable name in one place but not another).\n\n### Creating the get.var function\n\nAn R function is written below to extract a variable (e.g. *wp_nonfunctional*) as a vector out of an `sf` data frame.\n\n-   Arguments: `vname` (variable name as character, in quotes), `df` (name of `sf` data frame).\n\n-   Returns: `v`, a vector of values without a column name.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nget.var <- function(vname, df) {\n  v <- df[vname] %>%\n    st_set_geometry(NULL)\n  v <- unname(v[, 1])\n  return(v)\n}\n```\n:::\n\n\n### A percentile mapping function\n\nNext, a percentile mapping function is written using the code chunk below.\n\n\n::: {.cell}\n\n```{.r .cell-code}\npercentmap <- function(vnam, df, legtitle = NA, mtitle = \"Percentile Map\") {\n  percent <- c(0, .01, .1, .5, .9, .99, 1)\n  var <- get.var(vnam, df)\n  bperc <- quantile(var, percent)\n  tm_shape(df) +\n    tm_polygons() +\n  tm_shape(df) +\n    tm_polygons(vnam,\n            title = legtitle,\n            breaks = bperc,\n            palette = \"Blues\",\n            labels = c(\"< 1%\", \"1% - 10%\", \"10% - 50%\",\n                       \"50% - 90%\", \"90% - 99%\", \"> 99%\")) +\n    tm_borders() +\n    tm_layout(main.title = mtitle,\n              title.position = c(\"right\", \"bottom\"))\n}\n```\n:::\n\n\n### Test drive the percentile mapping function\n\n\n::: {.cell}\n\n```{.r .cell-code}\npercentmap(\"total_wp\", NGA_wp)\n```\n\n::: {.cell-output-display}\n![](Hands-on_Ex09c_files/figure-html/unnamed-chunk-10-1.png){width=672}\n:::\n:::\n\n\nNote that this is just a bare-bones implementation - additional arguments such as the title and legend positioning could be passed to customise other features of the map.\n\n## Box map\n\nIn essence, a box map is an augmented quartile map with an additional lower and upper category. When there are lower outliers, the starting point for the breaks is the minimum value, and the second break is the lower fence. When there are no lower outliers, the starting point for the breaks is the lower fence, and the second break is the minimum value (there will be no observations falling between the lower fence and the minimum value).\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(data = NGA_wp,\n       aes(x = \"\",\n           y = wp_nonfunctional)) +\n  geom_boxplot()\n```\n\n::: {.cell-output-display}\n![](Hands-on_Ex09c_files/figure-html/unnamed-chunk-11-1.png){width=672}\n:::\n:::\n\n\nA box map displays summary statistics on a choropleth map by using the basic principles of a boxplot. To create one, a custom breaks specification is needed - this is more complex than it sounds, because the break points vary depending on whether lower or upper outliers are present.\n\n### Creating the boxbreaks function\n\nThe code chunk below is an R function that creates break points for a box map.\n\n-   Arguments: `v` (vector of observations), `mult` (multiplier for IQR, default 1.5).\n\n-   Returns: `bb`, a vector with 7 break points computed from the quartiles and fences.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nboxbreaks <- function(v, mult = 1.5) {\n  qv <- unname(quantile(v))\n  iqr <- qv[4] - qv[2]\n  upfence <- qv[4] + mult * iqr\n  lofence <- qv[2] - mult * iqr\n  # initialize break points vector\n  bb <- vector(mode = \"numeric\", length = 7)\n  # logic for lower and upper fences\n  if (lofence < qv[1]) {  # no lower outliers\n    bb[1] <- lofence\n    bb[2] <- floor(qv[1])\n  } else {\n    bb[2] <- lofence\n    bb[1] <- qv[1]\n  }\n  if (upfence > qv[5]) { # no upper outliers\n    bb[7] <- upfence\n    bb[6] <- ceiling(qv[5])\n  } else {\n    bb[6] <- upfence\n    bb[7] <- qv[5]\n  }\n  bb[3:5] <- qv[2:4]\n  return(bb)\n}\n```\n:::\n\n\n### Creating the get.var function\n\nThe code chunk below is an R function to extract a variable as a vector out of an `sf` data frame.\n\n-   Arguments: `vname` (variable name as character, in quotes), `df` (name of `sf` data frame).\n\n-   Returns: `v`, a vector of values without a column name.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nget.var <- function(vname, df) {\n  v <- df[vname] %>% st_set_geometry(NULL)\n  v <- unname(v[, 1])\n  return(v)\n}\n```\n:::\n\n\n### Test drive the newly created function\n\n\n::: {.cell}\n\n```{.r .cell-code}\nvar <- get.var(\"wp_nonfunctional\", NGA_wp)\nboxbreaks(var)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] -56.5   0.0  14.0  34.0  61.0 131.5 278.0\n```\n\n\n:::\n:::\n\n\n### Boxmap function\n\nThe code chunk below is an R function to create a box map.\n\n-   Arguments: `vnam` (variable name as character, in quotes), `df` (simple features polygon layer), `legtitle` (legend title), `mtitle` (map title), `mult` (multiplier for IQR).\n\n-   Returns: a tmap element that plots a map.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nboxmap <- function(vnam, df,\n                    legtitle = NA,\n                    mtitle = \"Box Map\",\n                    mult = 1.5) {\n  var <- get.var(vnam, df)\n  bb <- boxbreaks(var)\n  tm_shape(df) +\n    tm_polygons() +\n  tm_shape(df) +\n    tm_fill(vnam, title = legtitle,\n            breaks = bb,\n            palette = \"Blues\",\n            labels = c(\"lower outlier\",\n                       \"< 25%\",\n                       \"25% - 50%\",\n                       \"50% - 75%\",\n                       \"> 75%\",\n                       \"upper outlier\")) +\n    tm_borders() +\n    tm_layout(main.title = mtitle,\n              title.position = c(\"left\", \"top\"))\n}\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntmap_mode(\"plot\")\nboxmap(\"wp_nonfunctional\", NGA_wp)\n```\n\n::: {.cell-output-display}\n![](Hands-on_Ex09c_files/figure-html/unnamed-chunk-16-1.png){width=672}\n:::\n:::\n\n",
    "supporting": [
      "Hands-on_Ex09c_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}